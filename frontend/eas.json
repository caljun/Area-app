{
  "cli": {
    "version": ">= 5.9.1",
    "appVersionSource": "app.json"
  },
  "build": {
    "production": {
      "developmentClient": false,
      "distribution": "store",
      "ios": {
        "simulator": false,
        "autoIncrement": false
      },
      "buildCommand": "npx expo export --dev --output-dir=./dist"
    }
  }
}
